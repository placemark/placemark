---
"@placemarkio/play": minor
---

Replace Mapbox GL JS and Deck.gl with MapLibre GL JS 6 and native MapLibre editing-point layers. Legacy Mapbox basemap configurations now prompt users to select a MapLibre replacement instead of loading Mapbox-hosted styles.

Use OpenFreeMap Positron as the token-free default basemap and support custom MapLibre style URLs.

Replace Mapbox Directions with an optional OSRM-compatible routing endpoint.
